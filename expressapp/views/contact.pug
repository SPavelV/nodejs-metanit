extends ./layouts/layout.pug

block title
    title #{title}

block content 
    h1 #{title}

    if emailsVisible 
        h3 Электронные адреса

        ul
            each email in emails 
                li #{emails}
    else
        h3 Электронный адрес отстутствует
    p Телефон: #{phone}